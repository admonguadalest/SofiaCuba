create table TEST1_DIRECCION (
    ID integer,
    UUID varchar(32),
    --
    NOMBRE varchar(255) not null,
    NUMERO varchar(50) not null,
    CODIGO_POSTAL varchar(25),
    --
    primary key (ID)
);